# 持仓表格组件化与未来行重排序方案

## 背景结论

当前持仓总览页的表格，已经具备以下能力：

- 纵向滚动时表头固定
- 横向滚动时第一列固定
- 列显示/隐藏
- 列顺序配置
- 表格滚动位置恢复

也就是说，从交互能力上看，当前实现已经覆盖了 `Sticky header table + Sticky column table` 的组合效果。

后续优化的重点，不是“能不能实现 sticky 能力”，而是：

1. 将持仓表格从页面中提取成独立组件
2. 组件内部的基础表格结构改为基于 `shadcn/ui` 的 Table primitive
3. 为未来“基金行重排序”预留清晰的数据结构和页面入口

---

## 现状判断

### 当前持仓页已经实现的 sticky 方案

当前持仓页实际使用的是 `src/app/portfolio/page.tsx` 内联表格，不是旧的 `src/components/portfolio-table.tsx`。

现有实现里已经包含：

- 左上角表头单元格：同时 `sticky top-0 left-0`
- 其他表头单元格：`sticky top-0`
- 第一列数据单元格：`sticky left-0`

因此当前已经具备：

- 上下滚动时 header 固定
- 左右滚动时第一列固定

### 当前页面还包含的业务复杂度

持仓页表格不是简单展示层，当前还承担了较多业务和交互逻辑，包括：

- 行数据计算与格式化
- 列显隐状态管理
- 列顺序持久化
- 列拖拽排序
- 表格滚动位置记忆与恢复
- 第一列基金跳转入口
- 收益颜色、估值/官方状态展示
- 不同列的更新时间展示

因此，后续组件化时应尽量保持“小步重构”，不要把业务逻辑和 UI 重构混在一起大改。

---

## 组件化目标

## 总体目标

将当前持仓页中的表格渲染部分提取为独立组件，例如：

- `src/components/portfolio-overview-table.tsx`

并让组件内部的基础 table 结构改为 `shadcn/ui` 风格的 primitive，例如：

- `Table`
- `TableHeader`
- `TableBody`
- `TableRow`
- `TableHead`
- `TableCell`

### 组件化的目的

1. 让 `portfolio/page.tsx` 只负责页面编排、状态管理和数据准备
2. 让表格渲染逻辑集中，后续 sticky、样式、交互更容易维护
3. 为以后增加行排序、表格变体、复用到其他页面打基础
4. 将“基础表格结构”和“持仓业务逻辑”分层

---

## 为什么组件内部基于 shadcn/ui

### 适合使用 shadcn primitive，但不适合直接照搬 demo

`shadcnstudio` 页面里的 table demo，本质上也是基于原生 table + Tailwind 的样式组合：

- sticky header: `sticky top-0`
- sticky column: `sticky left-0`

这说明其能力本质上是样式层能力，而不是复杂数据表引擎能力。

因此更适合的做法是：

1. 引入本地 `shadcn/ui` 风格的 `table.tsx`
2. 用它作为基础壳层
3. 保留项目自己的业务列渲染、sticky class、滚动恢复和排序逻辑

不建议直接把 demo 代码整体照搬到持仓页，因为 demo 并不包含当前页面已有的业务细节。

### 使用 shadcn primitive 的收益

- 结构更统一
- 语义更清晰
- 表格基础样式更集中
- 后续维护 sticky header / sticky column 更容易
- 未来若扩展为更多业务表格，可复用统一 table 基础层

### 使用 shadcn primitive 不能自动解决的问题

需要明确，换成 `shadcn/ui` 之后，不会自动解决以下问题：

- 行排序
- 列排序
- 滚动位置恢复
- 复杂单元格内容
- 云同步
- 用户偏好存储

这些仍然要依赖项目自身实现。

---

## 推荐的组件拆分方式

### 页面层职责

`src/app/portfolio/page.tsx` 继续负责：

- 从全局状态读取基金、持仓、交易数据
- 计算 rows
- 计算 totals
- 维护列显隐、列顺序、弹窗开关等页面状态
- 维护滚动位置持久化
- 将最终渲染所需数据传给表格组件

### 表格组件职责

新的持仓表格组件负责：

- 表头渲染
- 第一列渲染
- 普通列渲染
- sticky class 应用
- 单元格结构展示
- 行点击跳转结构
- 与外部透传滚动事件和容器 ref

### 推荐的 props 方向

可考虑将组件设计为只接收“已准备好的渲染数据”，避免组件内重复承担业务计算：

- `rows`
- `visibleColumns`
- `onOpenColumnConfig`
- `onRowNavigate`
- `onScrollPositionChange`
- `initialScrollTop`
- `initialScrollLeft`

这样可以保持组件职责单一。

---

## sticky 方案在新组件中的保留方式

迁移到组件后，sticky 方案可以原样保留，不需要改变交互模式。

### 推荐保留规则

- 左上角单元格：
  - `sticky top-0 left-0 z-20`
- 其他表头：
  - `sticky top-0 z-10`
- 第一列内容：
  - `sticky left-0 z-[1]`

### 注意点

1. sticky 单元格必须有明确背景色，否则滚动时会透出底层内容
2. 左上角单元格层级必须高于：
   - 普通表头
   - 首列 body cell
3. 外层滚动容器必须是同一个 `overflow-auto` 容器，不能把横向和纵向滚动拆成两个独立层

---

## 推荐的重构顺序

### 第一阶段：提取组件，不改行为

目标：

- 只把当前内联 `<table>` 抽成独立组件
- 不修改业务逻辑
- 不修改状态结构
- 不引入行排序

原则：

- 先保证行为完全一致
- 只做结构整理

### 第二阶段：组件内部切换到 shadcn/ui primitive

目标：

- 新建本地 `components/ui/table.tsx`
- 让持仓表格组件使用 `Table` 等 primitive
- 保留当前 sticky class 与布局规则

原则：

- 只替换结构层
- 不重写渲染逻辑
- 不改变持久化行为

### 第三阶段：引入“基金行顺序”偏好

目标：

- 在展示层增加用户可配置的基金顺序
- 先影响持仓页展示顺序
- 不改变基金业务数据本身

### 第四阶段：新增单独“持仓排序”页面

目标：

- 提供专门的拖拽排序入口
- 避免在主表格中混入复杂拖拽手势
- 提升移动端可用性

---

## 未来基金行重排序方案

## 需求判断

未来需要的是“基金行顺序重排序”，不是列顺序重排。

这个能力不建议直接在主表格里做拖拽，推荐单独一个排序页面，例如：

- `/portfolio/reorder`

### 为什么不建议直接在主表格拖拽

主表格当前已有以下高密度交互：

- 上下滚动
- 左右滚动
- sticky header
- sticky 第一列
- 点击进入基金详情
- 列配置
- 列顺序设置

如果再加入行拖拽，尤其在移动端，会带来明显冲突：

- 容易误触横向滑动/纵向滚动
- 容易误触点击进入详情
- 首列 sticky 区域与拖拽把手命中区域可能重叠
- 自动滚动、触摸拖拽体验复杂度高

因此，单独排序页是更稳妥的方案。

---

## 行顺序数据应该存在哪里

### 推荐：作为 UI 偏好存储

建议新增一个本地存储键，例如：

- `real-fund-mobile:portfolio-row-order`

数据格式：

```json
["023298", "006327", "019260"]
```

### 为什么不建议直接依赖 `state.funds` 的数组顺序

虽然当前 `funds` 数组天然有顺序，但不建议把“用户自定义显示顺序”直接耦合为核心业务数据，原因包括：

1. `funds` 主要表达“有哪些基金”，不是“用户展示偏好”
2. 云端合并逻辑更偏向去重合并，不是强顺序语义
3. 未来如果某些更新、导入、合并重建了 `funds`，顺序语义容易被破坏
4. 行顺序本质上更接近列顺序、列显隐，属于 UI preference

### 推荐做法

将 `portfolio-row-order` 纳入现有重要偏好体系，与以下内容同层：

- 持仓列显隐
- 持仓列顺序
- 市场页自选指数
- 手动同步时间戳

这样它可以：

- 本地持久化
- 跟随现有 preference 一起云同步
- 与业务数据解耦

---

## 行顺序的应用规则

### 基本规则

展示持仓行时，先按用户保存的 `rowOrder` 排序，再补齐未出现在顺序列表中的基金。

### 推荐排序逻辑

1. 读取当前所有基金 code
2. 读取本地保存的 `rowOrder`
3. 过滤掉已经不存在的 code
4. 按 `rowOrder` 顺序排列已命中的基金
5. 将未包含在 `rowOrder` 中的新基金追加到末尾

### 这样做的收益

- 兼容新增基金
- 兼容删除基金
- 老偏好不容易失效
- 不需要每次都重写完整顺序

---

## 新增基金和删除基金时的细节规则

### 新增基金

推荐规则：

- 用户新增基金后，如果该基金不在 `rowOrder` 中，自动追加到末尾

这样行为最稳定，符合用户对“手动排序后继续增量维护”的预期。

### 删除基金

推荐规则：

- 删除基金后，不需要立刻强制清洗 `rowOrder`
- 展示时忽略不存在的 code 即可
- 在合适时机可做一次轻量清洗并持久化

这样可以减少不必要写入，也让逻辑更容错。

---

## 单独排序页的交互建议

## 页面目标

单独排序页只做一件事：

- 调整基金展示顺序

不需要承载完整表格能力。

### 页面展示建议

每一项展示：

- 基金名称
- 基金代码
- 持仓金额
- 拖拽把手

必要时可附加：

- 今日收益
- 累计收益

但第一版建议保持轻量，只保留帮助用户识别基金的核心信息。

### 页面操作建议

可提供以下能力：

- 拖拽排序
- 恢复默认顺序
- 保存并返回
- 取消返回

可选增强能力：

- 按持仓金额排序
- 按今日收益排序
- 按累计收益排序

但这些建议做成“一键应用”的辅助操作，不要替代用户手动顺序。

---

## 拖拽技术实现建议

## 第一版建议

第一版推荐使用轻量方案：

- 原生 drag / pointer 事件
- 自定义 ghost
- 简单命中判断
- 列表项位置交换

原因：

- 项目当前列顺序拖拽已经有一套相近思路
- 排序页是单列列表，复杂度远低于主表格
- 可以先以最小代价验证需求

### 第一版实现重点

1. 拖拽把手区域足够大
2. 移动端 pointer 体验稳定
3. 拖拽时有占位反馈或浮层反馈
4. 长列表时支持自动滚动
5. 完成拖拽后立即更新本地顺序状态

## 后续增强方案

如果后续对拖拽手感要求更高，再考虑专门拖拽库，解决：

- 自动滚动
- 碰撞检测
- 动画过渡
- 键盘无障碍排序
- 移动端手势细节

第一版没有必要一开始就上重型方案。

---

## 主持仓页如何接入行顺序

## 接入时机

应在 `rows` 业务计算完成后，再做一次展示排序处理。

### 推荐顺序

1. 原始基金与持仓数据 -> `buildRows(...)`
2. 得到业务完整的 `rows`
3. 用 `rowOrder` 对 `rows` 做展示层重排
4. 将排序后的 `rows` 传给表格组件

### 这样做的好处

- 不影响原有业务计算逻辑
- 不影响 totals 统计口径
- 不改变已有字段结构
- 只在渲染前做展示顺序处理，风险小

---

## 与云同步的关系

## 推荐方案

将 `portfolio-row-order` 加入“重要 UI 偏好”列表，让它和列显隐、列顺序一样走现有同步通道。

### 这样做的收益

- 用户在不同设备登录后，持仓展示顺序一致
- 与现有 preference 机制统一
- 不需要改造核心云端业务数据结构

### 注意点

由于行顺序只是偏好，不是核心数据，云端冲突时可以沿用 preference 现有策略，不必引入复杂冲突合并规则。

---

## 推荐的最终方案

## 方案概述

1. 将持仓总览表格从 `portfolio/page.tsx` 提取为独立组件
2. 组件内部采用 `shadcn/ui` 风格 Table primitive
3. 保留当前 sticky header + sticky first column 方案
4. 保留现有列显隐、列顺序、滚动恢复逻辑
5. 新增 `portfolio-row-order` 作为持仓行顺序偏好
6. 新增独立“持仓排序”页面进行基金行拖拽排序
7. 主持仓页在渲染前按 `rowOrder` 应用展示顺序

---

## 实施顺序建议

### 第一阶段

- 抽离持仓表格组件
- 不改行为
- 不改状态结构

### 第二阶段

- 引入本地 `shadcn/ui` table primitive
- 将表格组件底层结构切换为 `shadcn` 风格
- 保持现有 sticky 和样式规则

### 第三阶段

- 新增 `portfolio-row-order` 偏好
- 将其纳入重要偏好同步
- 主持仓页接入该顺序

### 第四阶段

- 新增独立“持仓排序”页面
- 实现基金行拖拽排序
- 增加恢复默认顺序等辅助操作

---

## 核心原则

整个演进过程中应坚持以下原则：

1. 先抽组件，再换底层 UI primitive，再加新能力
2. 不要为了 `shadcn` 化而重写已有业务逻辑
3. 行排序作为展示偏好实现，不污染核心业务数据
4. 主表格优先保持浏览稳定性，拖拽放到独立页面
5. 新基金默认追加到末尾，保持用户心智稳定

---

## 一句话结论

当前持仓表格已经具备 sticky header 和 sticky first column 的组合能力；后续最合理的路线，是先提取成独立组件并在组件内部基于 `shadcn/ui` 的 Table primitive 重构结构，再将“基金行顺序”作为可同步的 UI 偏好，通过独立排序页面实现拖拽调整，而不是直接在主表格里做行拖拽。
